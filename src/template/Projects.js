import { useMemo, useState } from 'react';
import { Button, CTA, EmptyState, Icon, PageHero, StatusBadge } from '../components/SiteUI';
import { projects } from '../data/siteData';
const Projects=({onQuote})=>{const [query,setQuery]=useState('');const [cat,setCat]=useState('All');const [selected,setSelected]=useState(null);const visible=useMemo(()=>projects.filter(p=>(cat==='All'||p.category===cat)&&(`${p.title} ${p.client} ${p.industry}`).toLowerCase().includes(query.toLowerCase())),[query,cat]);return <main><PageHero eyebrow="Selected projects" title="Strategic solutions delivering measurable business impact." text="Explore how we combine technology, design, and focused strategy to solve real business challenges."><Button onClick={onQuote}>Start a project <Icon name="arrow"/></Button></PageHero><section className="section-pad"><div className="container"><div className="filter-bar"><div className="search-field"><Icon name="search"/><input aria-label="Search projects" placeholder="Search projects or industries…" value={query} onChange={e=>setQuery(e.target.value)}/></div><div className="filter-pills">{['All','Software','Branding','E-commerce','Marketing'].map(x=><button className={cat===x?'active':''} onClick={()=>setCat(x)} key={x}>{x}</button>)}</div></div>{visible.length?<div className="portfolio-grid-new">{visible.map(p=><article key={p.id}><div className="project-image-new"><img src={p.image} alt={p.title}/><StatusBadge>{p.category}</StatusBadge></div><div><small>{p.client} · {p.industry}</small><h2>{p.title}</h2><p>{p.description}</p><div className="tag-row">{p.tags.map(x=><span key={x}>{x}</span>)}</div><button onClick={()=>setSelected(p)}>View case study <Icon name="arrow" size={16}/></button></div></article>)}</div>:<EmptyState title="No projects found"/>}</div></section><CTA onQuote={onQuote}/>{selected&&<div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&setSelected(null)}><section className="case-modal" role="dialog" aria-modal="true" aria-label={`${selected.title} case study`}><button className="modal-close" onClick={()=>setSelected(null)} aria-label="Close">×</button><img src={selected.image} alt={selected.title}/><div><StatusBadge>{selected.category}</StatusBadge><h2>{selected.title}</h2><p>{selected.description}</p><div className="case-columns"><div><small>Challenge</small><p>Create a clearer customer experience while improving business performance and team efficiency.</p></div><div><small>Solution</small><p>A focused strategy, modern interface, and scalable implementation shaped around real customer needs.</p></div><div><small>Result</small><strong>{selected.result}</strong></div></div><Button onClick={onQuote}>Build something similar <Icon name="arrow"/></Button></div></section></div>}</main>};export default Projects;
